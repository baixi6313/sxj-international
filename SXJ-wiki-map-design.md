# 事现鉴 · 维基与地图功能设计（spec 草案）

> 状态：DRAFT（待白玺定稿）
> 日期：2026-08-08
> 定位：把"维基百科功能"与"地图功能"显性化，作为事现鉴（SXJ）三层视图之一。

---

## 0. 一句话定位

事现鉴不另建一个"维基百科"或"地图 App"，而是把同一套**可验证事现数据**用三种视图呈现：

- **验证协议层**（公共验算尺 / MAIP）—— 定义"什么叫可验证"
- **事现条目层**（维基式）—— 把每个事现写成结构化、可引用的条目
- **光锥坐标层**（地图）—— 把条目钉到时空位置、画出密度与区域

三者都是 **spec（格式规范）**，供各平台照着渲染；SXJ 自身**不持有中央内容库**。这守住"催化剂 / 中立桌子、功成身退"的定位。

---

## 1. 为什么是这三层（与 SXJ 内核的关系）

| 来源 | 内核 |
|---|---|
| 维基百科 | 可验证的公共事实（verifiability, not truth / 中立观点 / 有来源引用） |
| 地图 | 空间坐标 + 密度分布 |
| 事现鉴 | 基于 UDHR 第22条的可验证公共事实协议 + 光锥坐标 (x,y,z,t,ρ) |

结论：**维基与地图不是外加模块，是 SXJ 内核的两种天然视图**。问题不是"加两个功能"，而是"把这副骨架长成肉"。

---

## 2. 三层架构

```
                ③ 光锥坐标层 · 地图
                   ρ 热力 · 法域 j · 平衡区/交叉区 · 时间轴 t 回放
                          ▲ 落位地图
                ② 事现条目层 · 维基
                   光锥坐标 + 多当事人三值 + 验证包裹 + 来源链 + 版本史
                          ▲ 生成条目
                ① 验证协议层 · 公共验算尺
                   MAIP v1.0 · R-1..R-6 · ρ 度量标准
                          ▲
                 一个事现事件 evt_xxx
```

事件流：`evt_xxx` → 协议层量出 ρ → 生成维基条目 → 落位到地图。

---

## 3. 维基层：事现条目规范（SXJ-WIKI）

### 3.1 条目结构（每张卡字段）

| 字段 | 含义 | 对应 SXJ 概念 |
|---|---|---|
| 条目坐标 | (x, y, z, t, ρ) | 光锥坐标；比标题更精确，是时空定位 |
| 多当事人三值归因 | 枚举全部当事人，分别归 A(共济值)/B(贡献值)/C(负贡献) | 三值；单一视角不足定类 |
| 验证包裹 | sha256 + 自包含双语证书 | 可独立验证的"公证件" |
| 来源链 | 取数平台 / 证据 | 维基的"引用"；SXJ 引用的是可验证包裹 |
| 版本史 | ledger 哈希链，追加不可改 | 维基的编辑历史；SXJ 用账本更硬 |

**示例条目（evt_009）：**
- 坐标：(x, y, z, 2026-08-02, ρ=0.5355)
- 多当事人三值：耿同学本人 = 共济值 A（护真相）；被揭露方 = 负贡献（学术不端）
- 验证包裹：sha256 68a75820…（自包含双语证书）
- 来源链：WorkBuddy + Gemini 可取数 / Copilot 打不开 → 运输层断裂
- 版本史：ledger 哈希链 · 人类保留裁定 R-1..R-6

### 3.2 关键边界：验证门控 ≠ 开放编辑

这是整个维基层最重要的设计判断：

- **继承**维基百科的"可验证 / 有来源 / 中立"原则；
- **但不开放任意编辑**。条目内容的成立，靠**多当事人验证 + 公共验算尺量 ρ**，而非投票或共识。
- 这与 **D1 裁定**完全一致：同一事件对不同当事人可并存 A 与负贡献，靠"更大范围的确立"（枚举全部当事人、分别归值、跨账本对齐）定类，而非单一视角。

> 一句话：维基百科是一本书，谁都能改、靠共识；事现鉴的"维基"是一份**带公证的卷宗**——内容也是协作出来的，但每条都经过公共验算尺量过 ρ、封了包、上了链。

### 3.3 与现有资产的关系

- `events.html` 现为扁平列表 → 升级为"事现条目"视图的天然起点。
- MAIP v1.0 即本层的"编辑 / 引用政策"（定义什么算可验证来源）。

---

## 4. 地图层：光锥坐标可视化规范

### 4.1 基础坐标

- 平面 (x, y) = 经纬度；(z) 可表高度 / 层级；(t) = 时间轴回放；(ρ) = 点 / 区的颜色与大小（越实越亮）。
- 法域维度 **j**：切换 `中国含港澳台` / `国外`。

### 4.2 区域判据（核心创新）

- **平衡区**：光锥内部高 ρ 稳定轨迹 → 蓝 / 金 平静区。
- **交叉区**：光锥边界低 / 过渡 ρ 的碰撞 → 红 冲突区。
- 推论：**事现鉴的地图本质上是一张"社会稳定性热力图"**。

### 4.3 验证节点

- 标注已"坐到桌上"的平台 / 当事人，一眼看出"谁来了、谁还没来"——直接服务"催化剂拉新"的运营目标。

### 4.4 与现有资产的关系

- App 原型已有世界地图（含 j 法域维度、中国含港澳台红金高亮）。
- 待补：ρ 热力、平衡区 / 交叉区着色、时间轴 t 回放、验证节点标注。

---

## 5. spec 化原则（守护 root→spec / 催化剂定位）

- SXJ **只定义格式，不存内容**。各平台按 spec 渲染自己的事现鉴视图（维基条目 + 地图）。
- 协议一旦由平台共定，SXJ 功成身退——与既有"催化剂 / 中立桌子"定位完全一致。
- 因此维基与地图层都不是"SXJ 的数据库"，而是"任何平台都能照着画出的图纸"。

---

## 6. 开放待定项（不阻塞本设计，但影响落地质量）

| 项 | 影响 |
|---|---|
| G-2 A+B 权重 | 影响 ρ 与三值的具体算法 |
| G-3 冷方 1·2 | 影响验证节点的冷启动 |
| G-4 诱饵池 | 影响抗污染机制 |
| A+B 数值计分 | 仍挂起，条目暂为"台账"非"计分" |
| **G-9 运输层断裂** | 直接影响"来源链"跨平台取数一致性——是维基可信度的根基风险 |
| 第三方门户分叉（agentos-app.net 旧快照） | spec 需对齐，避免分叉实现稀释共识 |

---

## 7. 落地路线建议

1. **本设计文档定稿**（当前步）—— 把三层、条目格式、地图规则写成 spec 草案。
2. **维基条目模板原型** —— 升级 `events.html` 或 App 条目视图为"事现条目"卡。
3. **App 地图层增强** —— 在现有世界地图补 ρ 热力 / 平衡交叉区 / t 回放 / 节点标注。
4. **写进 MAIP** —— 把维基的"验证门控"规则、地图的"ρ 热力 / 法域维度"正式纳入协议，成为可采纳 spec。

---

---

## 8. 矩阵作为统一数据模型（坐标优先 · 2026-08-08 增补）

### 8.1 升级 §4.1 的坐标定义
原稿 §4.1 把 (x,y,z) 视为"经纬度 / 高度 / 层级"——偏地理。经 2026-08-08 讨论，坐标须显式承载"贡献类型与量级"：
- **(x, y, z) ＝ (三元: 自然/人力/数字, 三档: 上/中/下, 组织层级)**
- **j ＝ 法域**（中国含港澳台 / 国外）
- **ρ ＝ 可验证密度**
- **t ＝ 时间**
- **组织 ＝ 验证元数据**（谁在桌上）

原地理 (x,y)＝经纬度 仍有效，但**降为 j 轴（法域）内部的空间细节**，不与类型轴混淆。这样"三元×三档×j×组织"即构成高维验证场（≈ 9 × N × M），与既有光锥坐标 (x,y,z,t,ρ,j) 完全自洽。

### 8.2 矩阵 = 统一数据模型，三视图 = 投影
多元矩阵综合体（三元×三档×j×组织 + ρ + t）是事现鉴的**统一数据模型**：
- **维基视图（内容/条目层）** ＝ 投影 (三元,三档,j,组织) 为条目坐标 + 来源链 + 版本史 + 验证门控
- **地图视图（空间层）** ＝ 投影 j 为地理（中国含港澳台）+ t 回放 + 验证节点（组织"谁在桌上"）
- **热力图视图（密度层）** ＝ 投影 ρ 为颜色/大小 + 平衡区(蓝金)/交叉区(红)，且可"按三元×三档切片"

三者同源同一 evt_xxx，经 MAIP 量出 ρ 后落进矩阵坐标，同时被三视图渲染。

### 8.3 坐标系优先原则（本阶段只定义坐标，不预置计分）
白玺定调："所有数学问题都可以用坐标来解，目前阶段的事现鉴也是这个逻辑。"
→ 事现鉴当前阶段 ＝ **定义坐标 + 落点 + 量 ρ 的 spec，不预置任何聚合/计分**。公式/权重/跨国换算（原 §6 待定项 G-2 / G-3）全部**留给查询时的投影**，由各法域/各组织自行计算。这正呼应 **root→spec 功成身退**：把"怎么对齐坐标"定义成可采纳 spec，把"怎么求和"交还给世界。

### 8.4 与贡献值三元的关系
`贡献值-社会资源三元-讨论稿.md` 的"社会资源三元"在此获得坐标锚点：自然/人力/数字＝矩阵的 x 轴；三档＝y 轴；j＝法域。贡献值不再需要"完美全球绝对标尺"，只需每条事现精准落到 `(三元,三档,j,组织)+t+ρ`，聚合由投影完成。两稿互为支撑：讨论稿讲"三元里装什么（含中国 GEP 样板）"，本稿讲"三元往哪个坐标格落"。

*附：本设计源于 2026-08-08 与白玺的可视化讨论。前文部署 / COS 凭证 / D1 等问题按指示暂挂。*
