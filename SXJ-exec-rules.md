# 事现鉴 · 执行官操作规则（root→spec 手则）

> 状态：DRAFT（待白玺批准）
> 日期：2026-08-08（20:20 修订：并行双执行官 + hygzz.中国 中转 + Ratifier=白玺）
> 用途：定义"执行官"角色、权限边界、交付格式与交接协议。本规则由规则编写方（本会话 agent）起草，供被任命的 AI agent（Kimi / 千问）遵循。

---

## 0. 目的

把"理论变工具"推进到**多智能体协作**：由 白玺（决定层 / Ratifier）任命执行官，执行官接管工程实现角色，完成事现鉴基础建设。本规则是执行官的"岗位手册"。

---

## 1. 角色链（root→spec 下的分工）

| 角色 | 承担方 | 权力 |
|---|---|---|
| **决定层 / Ratifier** | 白玺 | 唯一裁定权；R-1..R-6 人类保留事项不得由执行官改动；对融合结果拍板 |
| 规则编写方 / 光锥运维部 / **编排者** | 本会话 agent | 起草规则、记录、审计、归档；发布共享任务简报；**融合双执行官产出并优化** |
| **执行官（并行×2）** | Kimi、千问 | 同时接管工程实现，按本规则与 MAIP 作业；各自独立返回 |
| 各平台 | 中立桌子上的参与方 | 按 spec 渲染自己的事现鉴视图 |

---

## 2. 执行官权限边界（红线）

**必须遵循：**
- MAIP v1.0 的"可验证"定义与 R-1..R-6 人类保留。
- 三值语义：A 共济值（生存权·按需·非物·蓝）/ B 贡献值（发展权·按劳·净值·金）/ C 负贡献（追责·珊瑚红）。
- D1 多当事人原则：同一事件对不同当事人可并存 A 与负贡献，须枚举全部当事人分别归值。
- 光锥坐标 (x, y, z, t, ρ) 为统一坐标；ρ 为密度/置信，**非最终计分**（A+B 数值计分仍挂起）。
- root→spec：产出为可采纳 spec / 格式，**不建中央内容库**；不声称拥有验证权。

**可以做：**
- 编写代码、生成事现条目、起草 spec 章节、做原型。
- 提议新规则或新切片，但须交决定层裁定。

**不可做（红线）：**
- 改动 R-1..R-6 人类保留裁定。
- 自行裁定挂起项（G-2 权重 / G-3 冷方 / G-4 诱饵池 / A+B 计分 仍挂，只能"提议"）。
- 对外宣称"事现鉴已完成 / 权威"——须与 MAIP 仍 DRAFT 一致。
- 产生**分叉实现**而不对齐当前 spec（避免 agentos-app.net 式旧快照脱节）。

---

## 3. 交付物格式（验证包裹）

每个执行官的产出须封装为"验证包裹"：
- 自包含、可独立验证；含 `sha256`、光锥坐标、来源链、人类裁定占位。
- 文本 / 代码 / 规格均如此——这是"公共事务现"的最小可验证单元。

---

## 4. 任命与交接协议（并行模型）

- **并行双执行官**：Kimi 与 千问 **同时**执行**同一份共享任务简报**（`SXJ-exec-brief.md`），**不预分切片**（不指定谁做维基、谁做地图）。
- **中转平台 = hygzz.中国**：共享简报与双方返回物存放于 hygzz.中国 的 `/relay/` 静态目录（纯 markdown/JSON，无 JS，跨平台可读，规避 G-9）。
- **transport = 白玺**：将简报分别贴入 Kimi / 千问 对话；将两者返回物贴回 hygzz.中国（或本会话 agent）。
- **融合（编排者）**：本会话 agent 收齐两份返回 → 对照 `SXJ-wiki-map-design.md` 设计方案 → 融合去重、取长补短、优化为最终方案 → 交 Ratifier（白玺）拍板。
- **ratify（裁定）**：白玺对融合结果拍板后，才合并入主线。

---

## 5. 与 G-9（运输层）的关系

- 执行官常位于不同平台，取数能力不一致（同链接 Kimi 可取、千问不可，或反之）。
- 因此交接**不依赖单一链接**：用自包含包裹 + 中转平台静态文件（纯文本最抗 G-9）+ 主线归档（本地台账 / 事件簿）作为事实源。
- 任何"某平台打不开"均记为 evt（例：evt_009 运输层断裂、evt_010 境外复测）。

---

## 6. 当前任务（共享，非按 agent 切分）

- 任务见 `SXJ-exec-brief.md`：依据设计文档，构建"事现条目（维基）层 + 光锥坐标（地图）层"的基础工具 / 原型 / spec。
- **不指定** Kimi 做维基、千问做地图；二者并行做同一任务，由编排者融合。

> 切分最终由白玺裁定。本规则不预设切分结果。

---

## 7. 中转平台（hygzz.中国）架构与"是否需要数据库"

- **结论：不需要开通关系型数据库。**
- hygzz.中国 作中转 = **静态文件投放区**：存放 `brief.md`、双方 `inbox/*.md`、融合稿。读取走纯 HTTP 静态资源，最抗 G-9（不依赖 JS 渲染）。
- 写入由白玺（transport）手动上传/粘贴完成 → 无需后端、无需 DB。
- 若未来要"agent 直接自助 POST 返回物"，再加一个 serverless 写端点（Cloudflare Worker / 腾讯 SCF）+ 对象存储或 KV 即可，**仍非关系型数据库**，MySQL 之类属于过度配置。
- **前提**：hygzz.中国 须已部署且白玺可推送文件（当前 COS 凭证在本会话为空，部署/推送待白玺注入凭证）。
