# hygzz.com 与 hygzz.中国 上线操作步骤（大白话版）

> 写给非技术背景的站长（白玺）。按步骤"去哪点、填什么"照做即可。
> 两个域名都走**腾讯云 COS 香港桶 + 腾讯云 DNSPod CNAME** 这条线，和 `hygzz.top` 是同一套架构、免备案。
> 你之前 `.top` 已经这样跑通了，所以这次是"再来两次同款操作"。

---

## 总览（先看清对应关系）

| 域名 | 用途 | 源站目录（要传的内容） | 新建桶名 | 桶所在地域 | DNS 在哪管 |
|---|---|---|---|---|---|
| **hygzz.中国** | 中转站 / 案例库 | `hygzz-cn-site/`（已备好） | `hygzzcn-1352601878` | 香港 ap-hongkong | DNSPod（腾讯云） |
| **hygzz.com** | 国际桌锚点（中英双语） | `hygzz-top-site/`（复用 .top 双语内容） | `hygzzcom-1352601878` | 香港 ap-hongkong | 看你 .com 注册时绑的 DNS（大多是 DNSPod/腾讯云，或阿里云/GoDaddy） |

> 桶名后缀 `1352601878` 是你 .top 同一个腾讯云账号的 APPID，保持一致方便管理。
> COS 香港桶 = **免 ICP 备案**，这点最重要。

---

## 第一部分：hygzz.中国（4 步）

### 第 1 步：建存储桶
1. 登录腾讯云 → **对象存储 COS** → 存储桶列表 → 点 **创建存储桶**。
2. 名称：`hygzzcn-1352601878`（必须全局唯一，照抄）。
3. 地域：**香港（ap-hongkong）** ← 选香港，免备案。
4. 访问权限：**公有读私有写**（别人才能打开网页）。
5. 高级设置里勾 **"开启静态网站"**，索引文档填 `index.html`，错误文档也填 `index.html`。
6. 点完成。

### 第 2 步：把内容传上去
- 进桶 → 文件列表 → **上传** → 把本地目录 `hygzz-cn-site/` 下**所有文件和子目录**（`inbox/`、`cases/` 等）拖进去，保持目录结构。
- 不用动 `DEPLOY.md`、`brief.md` 这些说明文件，一起传上去也无害。

### 第 3 步：让域名指向桶（DNSPod 加记录）
1. 腾讯云 → **DNSPod** → 找到域名 `hygzz.中国` → **添加记录**：
   - 主机记录：`@`（代表根域名本身）
   - 记录类型：**CNAME**
   - 记录值：`hygzzcn-1352601878.cos-website.ap-hongkong.myqcloud.com`（就是第1步桶的"静态网站端点"，控制台桶概览里能复制）
   - 线路：默认
2. 保存，等 1–10 分钟（DNS 生效）。

### 第 4 步：验证
浏览器打开 `http://hygzz.中国/` → 看到红金首页即成功。

### （推荐）升级 HTTPS（让地址栏显示小锁）
1. 腾讯云 COS 控制台 → 进 `hygzzcn-1352601878` 桶 → **域名管理** → **添加自定义域名**，填 `hygzz.中国`。
2. 证书配置选 **"腾讯云托管证书"**（免费 DV 证书，自动签发）→ 勾选自动续期。
3. 提交后控制台会给你一个 **CDN 加速域名**（形如 `hygzz.中国.cdn.dnsv1.com`）。
4. 回到 **DNSPod**，把第 3 步那条 CNAME 的"记录值"改成这个 CDN 加速域名，保存。
5. 几分钟后 `https://hygzz.中国/` 就能 HTTPS 访问了。

---

## 第二部分：hygzz.com（4 步，同款）

### 第 1 步：建存储桶
1. 对象存储 COS → 创建存储桶。
2. 名称：`hygzzcom-1352601878`。
3. 地域：**香港（ap-hongkong）**（免备案）。
4. 访问权限：**公有读私有写**。
5. 勾 **"开启静态网站"**，索引文档 `index.html`、错误文档 `index.html`。

### 第 2 步：把内容传上去
- 把本地目录 **`hygzz-top-site/`** 下所有文件和子目录拖进新桶（这一套本来就是中英双语版，正好当国际桌）。
- ⚠️ 注意：该目录里有个 `CNAME` 文本文件写的是 `hygzz.top`，对 COS 无影响（COS 不读它），可忽略；若你洁癖想删，传完在桶里删掉这个文件即可。

### 第 3 步：让域名指向桶（在你 .com 的 DNS 后台加记录）
- **若 .com 也在 DNSPod/腾讯云**：和第 1 部分第 3 步一模一样，只是域名换成 `hygzz.com`，记录值填 `hygzzcom-1352601878.cos-website.ap-hongkong.myqcloud.com`。
- **若 .com 在别处**（阿里云/GoDaddy 等）：登录那个服务商的 DNS 管理，加一条 **CNAME** 记录，主机记录 `@`，值填上面的网站端点，保存。其余不变。
- 不知道 .com 的 DNS 在哪？看 .com 的"域名管理/whois"里的 Nameserver 指向谁，就是哪家。

### 第 4 步：验证
浏览器打开 `http://hygzz.com/` → 看到双语首页即成功。

### （推荐）升级 HTTPS
与第一部分"升级 HTTPS"完全同理：COS 桶 `hygzzcom-1352601878` → 域名管理 → 添加自定义域名 `hygzz.com` → 腾讯云托管证书 → 把 DNS 的 CNAME 值改成控制台给的 CDN 加速域名。

---

## 常见问题

**Q：一定要用香港桶吗？**
A：想免备案就用香港。若用广州桶，自定义域名需先做 ICP 备案（麻烦），不推荐。

**Q：CNAME 和"静态网站端点"长什么样？**
A：形如 `hygzzcn-1352601878.cos-website.ap-hongkong.myqcloud.com`。在 COS 控制台 → 桶概览 → "静态网站"卡片里能一键复制。

**Q：两个域名会不会内容撞车？**
A：不会。`.中国` 用的是 `hygzz-cn-site/`（中转站），`.com` 用的是 `hygzz-top-site/`（双语旗舰/国际桌），是两套独立内容、两个独立桶。

**Q：我不想自己点，想让我（编排者）代劳？**
A：把腾讯云 **SecretId / SecretKey** 经 WorkBuddy 的"密钥/环境变量"入口注入本会话，我用现成的 `tcb_*.py` 脚本一键推两个桶上线（和之前 .top 同款）。**密钥不要贴在聊天框明文**。

**Q：.com 想做成纯英文国际首页（不要中文）？**
A：现在先复用双语版跑通；之后我单独建一个 `hygzz-com-site/`（纯英文/国际版）再传，告诉我就行。

---

## 安全提醒
SecretKey / API Token **不要贴在聊天框明文**。优先从 WorkBuddy 的"密钥/环境变量"入口注入；若必须发，只发 SecretId，Key 走私密通道。
