# 事现鉴 · 执行官共享任务简报（并行双执行官）

> 配套必读：SXJ-exec-rules.md（你的红线与交付格式）、SXJ-wiki-map-design.md（设计蓝本）、SXJ-verification-ledger.md（现有账本与 D1 裁定）
> 模式：Kimi 与 千问 **同时**执行本简报，各自独立返回，由编排者（本会话 agent）融合优化。
> Ratifier：白玺（对融合结果拍板）

---

## 一、背景与目标

把事现鉴从"理论 / 协议"推进为"可验证工具"。本次基础建设目标：**让事现鉴显式具备维基百科式事实库 + 地图式空间呈现**两层能力，且严格遵循事现鉴内核（可验证、三值、光锥坐标、root→spec 不建中央库、验证门控非开放编辑）。

一句话：理论已经清楚，现在要做成能跑、能看的部件。

---

## 二、你必须先读的 3 份材料

1. **SXJ-exec-rules.md** —— 你的红线与交付格式（务必先读，违反红线即废标）。
2. **SXJ-wiki-map-design.md** —— 三层设计蓝本（重点 §3 维基层 / §4 地图层 / §3.2 验证门控边界）。
3. **SXJ-verification-ledger.md** —— 现有账本、D1 多当事人裁定、ρ 现状。

---

## 三、核心概念（对齐 spec，避免分叉）

- **事现鉴（SXJ）**：基于《世界人权宣言》第22条的可验证公共事实协议；终极目标 = 全球社保。"公共验算尺"是统一度量衡工具。
- **三值**：A 共济值（生存权·按需·非物·蓝）/ B 贡献值（发展权·按劳·净值·金）/ C 负贡献（追责·珊瑚红）。先天残衰病 ≠ 负贡献。
- **光锥坐标**：每个事现是时空点 (x, y, z, t, ρ)；ρ = 密度/置信，非计分。
- **D1 多当事人原则**：同一事件对不同当事人可并存 A 与负贡献，须枚举全部当事人分别归值（"更大范围的确立"）。
- **root→spec**：SXJ 是"可采纳规格"，不是中央实体；协议被平台共定后即功成身退。你不建中央库。
- **MAIP v1.0**：协议本身，仍 DRAFT；R-1..R-6 为人类保留裁定，你不可改。
- **验证包裹**：产出须自包含、可独立验证，含 sha256 + 坐标 + 来源链 + 人类裁定占位。

---

## 四、任务（你独立完成的全部内容）

依据设计蓝本，产出以下可验证部件（形式：可运行代码 + 自包含说明；或明确算法规范）。**鼓励你用自己的工程偏好实现**——因为编排者会融合两种思路取长补短。

1. **事现条目（维基）层原型**
   - 把 `events.html` 式的扁平列表升级为"事现条目"视图。
   - 字段（见设计文档 §3.1）：光锥坐标 (x,y,z,t,ρ) + 多当事人三值归因 + 验证包裹 sha256 + 来源链 + 版本史（ledger 哈希链）。
   - 用 `evt_009` / `evt_010` 作示例数据。
   - **关键边界**：维基 = **验证门控**，不是开放编辑（设计文档 §3.2）。

2. **地图层原型 / 规范**
   - 世界地图（含中国含港澳台红金高亮、j 法域维度）上呈现：
     - ρ 热力（点 / 区按 ρ 上色与缩放）
     - 平衡区（光锥内部高 ρ 稳定轨迹 → 蓝/金） vs 交叉区（边界碰撞 → 红）
     - 时间轴 t 回放（事件扩散）
     - 验证节点标注（已"坐桌"的平台 / 当事人）
   - 本质 = 一张"社会稳定性热力图"。

3. **（可选）MAIP spec 草案**：把上述两层写成可采纳协议章节。

---

## 五、交付格式（验证包裹）

- 自包含、可独立验证；含 `sha256`、光锥坐标、来源链、人类裁定占位。
- 返回物贴回中转平台 hygzz.中国 `/relay/inbox/<你的名>.md`，并交白玺（transport）。

---

## 六、红线（同规则 §2）

- 不改 R-1..R-6；不自行裁定挂起项（G-2/3/4、A+B 计分只能提议）。
- 维基 = 验证门控，非开放编辑。
- 不建中央内容库；对齐当前 spec，不分叉。

---

## 七、G-9 提醒

若你无法访问外部链接 / GitHub / 文档，以**自包含文本**交付并注明取数限制。交接不依赖单一链接；事实源以决定层归档为准。
